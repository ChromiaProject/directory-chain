module;

import ^^.*;

operation icmf_message(sender: byte_array, topic: text, body: gtv) {
    receive_icmf_message(sender, topic, body);
}

operation expire_leases_op(delta_millis: integer) {
    expire_leases(op_context.last_block_time + delta_millis);
}
