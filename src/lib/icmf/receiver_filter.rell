module;

import ^.constants.*;

@mount("icmf.")
query receiver_rate_limit(sender: byte_array, topic: text): map<text, gtv> {
    rate_limit_icmf(sender, topic_without_receiver(topic));
    return [:];
}

@extendable function rate_limit_icmf(sender: byte_array, topic: text) {}
