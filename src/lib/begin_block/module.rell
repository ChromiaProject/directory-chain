module;

struct module_args {
    detect_configuration_updates: boolean = false;
}

object begin_block {
    mutable current_configuration_hash: byte_array = x"";
}

operation __begin_block(height: integer) {

    being_block_detect_config_update();
    on_begin_block(height);
    begin_block_scheduled_callback();
}
