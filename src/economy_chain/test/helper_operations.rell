module;

namespace ft4 {
    import lib.ft4.accounts;
    import lib.ft4.assets;
}

import ^^.*;
import common_proposal.test.helper_operations.*;

operation begin_block_op(height: integer) {
    if (state.initialized) {
        _try_to_update_assets();
    }
}

operation icmf_message_op(sender: byte_array, topic: text, body: gtv) {
    handle_icmf_message(sender, topic, body);
}

operation empty_account_op(ft4.accounts.account) {
    val balance = get_account_balance(account);
    ft4.assets.Unsafe.transfer(account, get_pool_account(), get_asset(), balance);
}

operation set_account_balance_op(ft4.accounts.account, amount: big_integer) {
    update ft4.assets.balance @? { account, get_asset() } ( .amount = amount );
}

operation unsafe_transfer_op(from: ft4.accounts.account, to: ft4.accounts.account, amount: big_integer) {
    ft4.assets.Unsafe.transfer(from, to, get_asset(), amount);
}

operation mint_to(account: ft4.accounts.account, amount: big_integer) {
    ft4.assets.Unsafe.mint(account, get_asset(), amount);
}

operation mint_to_pool(amount: big_integer) {
    ft4.assets.Unsafe.mint(get_pool_account(), get_asset(), amount);
}

operation create_pending_cluster(
    cluster_name: text,
    tag_name: text,
    status: pending_cluster_status = pending_cluster_status.PENDING_APPROVAL,
    scu_price: integer = 1 * units_per_usd,
    extra_storage_price: integer = 1 * units_per_usd,
    container_unit_cpu: integer = 50,
    container_unit_ram: integer = 2048,
    container_unit_io_read: integer = 25,
    container_unit_io_write: integer = 20,
    container_unit_storage: integer = 16384,
    system_container_units: integer = 4,
) {

    val tag = create_or_return_tag(tag_name, scu_price = scu_price, extra_storage_price = extra_storage_price);
    val proposal = create_proposal(common_proposal_type.ec_cluster_create);

    create pending_cluster(
        proposal,
        name = cluster_name,
        tag,
        proposer_pubkey = x"",
        governor_voter_set_name = "governor_voter_set_name",
        voter_set_name = "voter_set_name",
        cluster_units = 1,
        extra_storage = 1,
        container_unit_cpu,
        container_unit_ram,
        container_unit_io_read,
        container_unit_io_write,
        container_unit_storage,
        system_container_units,
        status
    );
}

operation create_pending_tag_operation(name: text, scu_price: integer = -1,
        extra_storage_price: integer = -1, extra_compute_request_price: integer = -1) {

    val proposal = create_proposal(common_proposal_type.ec_tag_create);
    create pending_tag_operation(
        name,
        proposal,
        scu_price = scu_price,
        extra_storage_price = extra_storage_price,
        extra_compute_request_price = extra_compute_request_price,
    );
}

operation create_pending_cluster_change_tag(cluster_name: text, current_tag_name: text, new_tag_name: text) {

    val current_tag = create_or_return_tag(current_tag_name);
    val new_tag = create_or_return_tag(new_tag_name);
    val proposal = create_proposal(common_proposal_type.ec_cluster_create);

    val cluster = create cluster(cluster_name, true, cluster_units = 1, extra_storage = 1, anchoring_chain = x"", current_tag);
    create pending_cluster_change_tag(cluster, proposal, new_tag);
}

operation create_test_cluster(
    cluster_name: text,
    tag_name: text = "test-tag",
    cluster_units: integer = 1,
    extra_storage: integer = 1,
    scu_price: integer = 1 * units_per_usd,
    extra_storage_price: integer = 1 * units_per_usd,
    extra_compute_request_price: integer = 1 * units_per_usd,
) {
    val tag = create tag(tag_name, scu_price, extra_storage_price, extra_compute_request_price = extra_compute_request_price);
    create cluster(name = cluster_name, operational = true, cluster_units = cluster_units, extra_storage = extra_storage,
        anchoring_chain = x"", tag = tag);
}

operation create_test_cluster_with_tag(
    cluster_name: text,
    tag,
    cluster_units: integer = 1,
    extra_storage: integer = 1,
) {
   create cluster(name = cluster_name, operational = true, cluster_units = cluster_units, extra_storage = extra_storage,
        anchoring_chain = x"", tag = tag);
}

operation create_test_subnode_image(
    name: text,
    url: text = "url",
    digest: text = "digest",
    subnode_image_type = subnode_image_type.COMMON,
    owner: provider,
    description: text = "description",
    active: boolean = true,
    cluster_name: text = "",
) {
    val s = create subnode_image(name, url = url, digest = digest, subnode_image_type = subnode_image_type, owner, description, active);
    if (not cluster_name.empty()) {
        create cluster_subnode_image(cluster @ { .name == cluster_name }, s);
    }
}

operation create_test_cluster_subnode_image(
    cluster,
    subnode_image
) {
    create cluster_subnode_image(cluster, subnode_image);
}

operation create_test_subnode_jar_extension(
    name: text,
    subnode_jar_extension_type = subnode_jar_extension_type.COMMON,
    owner: provider,
    description: text = "description",
    active: boolean = true,
    cluster_name: text = "",
) {
    val s = create subnode_jar_extension(name, subnode_jar_extension_type, owner, description, active);
    if (not cluster_name.empty()) {
        create cluster_subnode_jar_extension(cluster @ { .name == cluster_name }, s);
    }
}

operation create_test_cluster_subnode_jar_extension(
    cluster,
    subnode_jar_extension
) {
    create cluster_subnode_jar_extension(cluster, subnode_jar_extension);
}

operation create_lease(container_name: text, container_units: integer, account: ft4.accounts.account, cluster: cluster, expired: boolean) {
    create lease(
        container_name = container_name,
        account = account,
        container_units = container_units,
        cluster = cluster,
        start_time = 0,
        duration_millis = 0,
        expired = expired,
        auto_renew = false,
        extra_storage_gib = 0,
        extra_compute_requests = 0,
    );
}

operation pay_rewards_op(cluster: cluster, node_availability_report: node_availability_report) {
    pay_rewards(cluster, node_availability_report);
}

operation pay_reward_op(pool: ft4.accounts.account, account: ft4.accounts.account, reward_debt:big_integer,  reward: big_integer, identity: text, delegation_share: decimal) {
    pay_reward(pool, account, reward_debt, reward, identity, delegation_share);
}

operation set_pool_details_last_refill_millis_last_block_time_op() {
    pool_details.last_refill_millis = op_context.last_block_time;
}

operation set_reward_debt_op(provider_pubkey: pubkey, reward_debt: big_integer) {
    val pa = provider_account @ { provider @ { provider_pubkey } };
    pa.reward_debt = reward_debt;
}

operation receive_create_cluster_error_message_op(cluster_name: text, error_message: text) {
    receive_create_cluster_error_message(create_cluster_error_message(name = cluster_name, error_message = error_message).to_gtv());
}

operation receive_cluster_update_message_op(message: cluster_update_message) {
    receive_cluster_update_message(message.to_gtv());
}

operation create_cluster_node_op(cluster, node) {
    create cluster_node(cluster, node);
}

operation create_cluster_replica_node_op(cluster, node) {
    create cluster_replica_node(cluster, node);
}

operation save_node_availability_op(report: node_availability_report) {
    save_node_availability(report);
}

operation save_node_payed_reward_op(node: byte_array, value: big_integer, time: timestamp) {
    save_node_payed_reward(node, value, time);
}

operation receive_provider_update_message_op(provider_update_message) {
    receive_provider_update_message(provider_update_message.to_gtv());
}

operation receive_provider_auth_update_message_op(provider_auth_update_message) {
    receive_provider_auth_update_message(provider_auth_update_message.to_gtv());
}

operation create_tag_removal_proposal_with_voter_set(voter_set_name: text) {
    create_proposal(common_proposal_type.ec_tag_remove, voter_set_name);
}

function create_or_return_tag(
    tag_name: text,
    scu_price: integer = 1 * units_per_usd,
    extra_storage_price: integer = 1 * units_per_usd,
    extra_compute_request_price: integer = 1 * units_per_usd,
) {
    var tag = tag @? { tag_name };
    if (tag == null) {
        tag = create tag(
            tag_name,
            scu_price = scu_price,
            extra_storage_price = extra_storage_price,
            extra_compute_request_price = extra_compute_request_price,
        );
    }
    return tag;
}

function create_or_return_provider(pubkey, system: boolean) {
    var provider = provider @? { pubkey };
    if (provider == null) {
        provider = create provider(pubkey, system = system, provider_tier.NODE_PROVIDER, active = true, name = "", url = "");
    }
    return provider;
}

operation create_price_oracle_rates_op(symbol: text, name: text, price: decimal) {
    set_token_rate(symbol, name, price);
}

operation do_test_evm_block(network_id: integer, evm_block_height: integer, evm_block_hash: byte_array, events: list<eif.messaging.event_data>) {
    _fn_evm_block(network_id, evm_block_height, evm_block_hash, events);
}

operation create_or_return_provider_op(pubkey, system: boolean) {
    create_or_return_provider(pubkey, system);
}

operation set_provider_staking_rewards_share(pubkey, staking_rewards_share: decimal) {
    update provider @ { pubkey } (.staking_rewards_share = staking_rewards_share);
}

operation create_proposal_op(proposal_type: common_proposal_type, voter_set_name: text) {

    val voter_set = create_or_return_voter_set(voter_set_name);
    val provider = create_or_return_provider(x"", true);

    create common_proposal(op_context.last_block_time, proposal_type, provider.pubkey, voter_set);
}

function create_proposal(proposal_type: common_proposal_type, voter_set_name: text = "voter-set") {

    val voter_set = create_or_return_voter_set(voter_set_name);
    val provider = create_or_return_provider(x"", true);

    return create common_proposal(op_context.last_block_time, proposal_type, provider.pubkey, voter_set);
}

operation register_provider_account_op(provider) {

    register_provider_account(provider);
}

operation pay_out_node_rewards_op(node_reward: map<node, big_integer>, has_delegation_share: boolean) {
    pay_out_node_rewards(node_reward, has_delegation_share);
}

operation pay_out_chromia_foundation_reward_op() {
    pay_out_chromia_foundation_reward();
}
operation add_staking(provider_staking_account, amount: integer, network: network = network.CHR) {
    val ss = create staker_state (
        account = provider_staking_account.staking_account,
        network = network,
        balance = amount,
        last_update = 0,
        pending_token_time = 0
    );
    create delegation_state(
        staker_state = ss,
        current_provider = provider_staking_account.provider,
        current_active_from = _staking_last_known_time(),
        next_provider = provider @ { get_null_account() },
        next_change_at = 0
    );
}

operation staking_init_op() {
    __staking_init();
}

operation set_last_approved_stake_requirements(psa: provider_staking_account, time: integer) {
    psa.last_approved_stake_requirements = time;
}

operation register_test_account(pubkey) {

    val auth_descriptor = ft4.accounts.single_sig_auth_descriptor(pubkey, set(["A", "T"]));

    ft4.accounts.create_account_with_auth(auth_descriptor);
}

operation update_system_provider_economy_constants(
    total_cost_system_providers: integer? = null,
    system_provider_fee_share: decimal? = null,
    system_provider_risk_share: decimal? = null
) {
    if (total_cost_system_providers != null) {
        economy_constants.total_cost_system_providers = total_cost_system_providers * units_per_usd;
    }
    economy_constants.system_provider_fee_share = system_provider_fee_share ?: economy_constants.system_provider_fee_share;
    economy_constants.system_provider_risk_share = system_provider_risk_share ?: economy_constants.system_provider_risk_share;
}

operation update_staking_requirements_economy_constants(
    staking_requirements_enabled: boolean? = null,
    staking_requirement_stop_payout_days: integer? = null,
    staking_requirement_system_node_own_stake_chr: integer? = null,
    staking_requirement_system_node_total_stake_chr: integer? = null,
    staking_requirement_dapp_node_own_stake_chr: integer? = null,
    staking_requirement_dapp_node_total_stake_chr: integer? = null
) {
    economy_constants.staking_requirements_enabled = staking_requirements_enabled ?: economy_constants.staking_requirements_enabled;
    economy_constants.staking_requirement_stop_payout_days = staking_requirement_stop_payout_days ?: economy_constants.staking_requirement_stop_payout_days;
    economy_constants.staking_requirement_system_node_own_stake_chr = staking_requirement_system_node_own_stake_chr ?: economy_constants.staking_requirement_system_node_own_stake_chr;
    economy_constants.staking_requirement_system_node_total_stake_chr = staking_requirement_system_node_total_stake_chr ?: economy_constants.staking_requirement_system_node_total_stake_chr;
    economy_constants.staking_requirement_dapp_node_own_stake_chr = staking_requirement_dapp_node_own_stake_chr ?: economy_constants.staking_requirement_dapp_node_own_stake_chr;
    economy_constants.staking_requirement_dapp_node_total_stake_chr = staking_requirement_dapp_node_total_stake_chr ?: economy_constants.staking_requirement_dapp_node_total_stake_chr;
}

operation create_reward_provider_deleted_node_op(provider, node: pubkey, cluster: cluster) {
    create_reward_provider_deleted_node(provider, node, [cluster]);
}

operation set_migrations_completed(
    provider_staking_account_entity: boolean = true,
    publish_staking_state: boolean = true,
) {
    migration_completed.provider_staking_account_entity = provider_staking_account_entity;
    migration_completed.publish_staking_state = publish_staking_state;
}

operation purge_provider_staking_account_entities() {
    delete provider_staking_account @* {};
}

operation do_migrations_op() {
    do_migrations();
}

operation poke_staker_state_update(staker_state) {
    _update_pending_token_time(staker_state);
}

operation poke_staker_state_update_all() {
    _update_pending_token_time_all();
}

operation pay_everything() {
    _staking_distribute_withdrawals();
    _staking_distribute_rewards();
}

operation pay_evm_transaction_submitter_rewards_op(
    evm_transaction_submitter_cost: evm_transaction_submitter_cost,
    tx_cost: big_integer) {
    pay_evm_transaction_submitter_rewards(evm_transaction_submitter_cost, tx_cost);
}

operation create_evm_account(evm_address: byte_array) {
    val auth_descriptor = ft4.accounts.single_sig_auth_descriptor(evm_address, set(["A", "T"]));
    val account = ft4.accounts.create_account_with_auth(auth_descriptor);
}

operation create_account_link(ft4.accounts.account, address: byte_array) {
    create eif.hbridge_core.evm_eoa_account_link ( account, address );
}

operation set_provider_multi_key_false() {
    migration_completed.provider_multi_key = false;
}

operation require_is_system_provider_and_signer_op(provider_pubkey: pubkey) {
    require_is_system_provider_and_signer(provider_pubkey);
}

operation set_system_p_member_key_vote_set() {
    system_p_voter_set().member_key_type = common_proposal_vote_type.VOTER_SET_MEMBER_KEY;
}

operation create_pending_shared_payment(account_id: byte_array, amount: integer, provider_pubkey: pubkey) {
    val account = ft4.accounts.Account(account_id);
    create pending_shared_payment(account, amount, provider_pubkey);
}

operation receive_ticket_container_result_message_op(body: gtv) {
    receive_ticket_container_result_message(body);
}

operation receive_resource_usage_statistics_message_op(body: gtv) {
    receive_resource_usage_statistics_message(body);
}

operation wipe_upgrade_container_tickets(container_name: text) {
    delete upgrade_container_ticket @* { container_name };
}

operation staking_request_withdrawal_on_network(account: ft4.accounts.account, network: network) {
    _staking_request_withdrawal(account, network);
}

operation remove_delegation_states() {
    delete delegation_state @* {};
}

operation override_existing_bridge(network_id: integer, bridge_address: byte_array) {
    // Let's fake that we have a change of bridge contracts with some manual interference with entities
    val existing_bridge_asset = eif.hbridge_erc20.erc20_asset @ {
        get_asset(),
        .network_id == network_id
    };
    val existing_bridge = eif.hbridge_erc20.bridge_erc20_asset @ {
        existing_bridge_asset
    };
    val existing_bridge_contract = existing_bridge.bridge_contract;
    delete existing_bridge;
    delete existing_bridge_contract;

    eif.hbridge_erc20.register_bridge_with_erc20_assets(
        network_id,
        bridge_address, //BOGUS
        0,
        [existing_bridge_asset]
    );
}

operation add_bridge(network_id: integer, bridge_address: byte_array, asset_address: byte_array) {
    eif.hbridge_erc20.register_bridge_and_erc20_asset(
        network_id,
        asset_address,
        get_asset(),
        eif.hbridge.bridge_mode.foreign,
        bridge_address = bridge_address,
        skip_to_height = 0
    );
}

operation try_to_update_assets() {
    _try_to_update_assets();
}

// Taking instant effect
operation set_provider_staking_reward_share(provider_key: pubkey, staking_rewards_share: decimal) {
    val provider = require_provider(provider_key);
    provider.staking_rewards_share = staking_rewards_share;
}

operation put_null_provider_in_legacy_state() {
    update provider @ { get_null_account() } (.active = true, .system = true);
}
