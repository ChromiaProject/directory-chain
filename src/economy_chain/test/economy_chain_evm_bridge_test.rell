@test module;

import ^^.*;

function test_add_erc20_link_on_init() {

	rell.test.tx().op(
			init()
		).run();

	// TODO: commented for now
//	assert_not_null(eif.ft4.token_mapping @? { get_asset() });
}
