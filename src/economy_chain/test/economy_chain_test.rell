@test module;

import ^^.*;

struct module_args {
    asset_decimals: integer = 6;
}

function test_constants() {
    assert_equals(units_per_asset, 1000000);
    assert_equals(units_per_usd, 1000000);
    assert_equals(units_per_txs_evm_cost, 1000000000000000000L);
}
