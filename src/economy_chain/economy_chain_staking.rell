// Provider management

function _staking_offboard_provider(pubkey) {
    val null_provider = provider @ { get_null_account() };
    val current_time = _staking_last_known_time();

    // Handle delegation states where next_change_at has passed and next_provider is this provider
    create auto_undelegation (
        delegation_state @* {
           .next_provider.pubkey == pubkey,
           .next_change_at > 0,
           .next_change_at <= current_time
        } (struct<auto_undelegation>($, pubkey))
    );
    update delegation_state @* {
        .next_provider.pubkey == pubkey,
        .next_change_at > 0,
        .next_change_at <= current_time
    } (
        .previous_provider = .next_provider,
        .current_provider = null_provider,
        .current_active_from = current_time,
        .next_provider = null_provider,
        .next_change_at = 0
    );


    // Cancel any upcoming delegation changes to this provider
    create auto_cancel_upcoming_delegation (
        delegation_state @* {
            .next_provider.pubkey == pubkey,
            .next_change_at > current_time
        } (struct<auto_cancel_upcoming_delegation> ($.staker_state.account, $.next_change_at, $, pubkey))
    );
    update delegation_state @* {
        .next_provider.pubkey == pubkey,
        .next_change_at > current_time
    } (
        .next_provider = null_provider,
        .next_change_at = 0
    );

    // Handle delegation states with this provider as the current provider
    create auto_undelegation (
        delegation_state @* {
            .current_provider.pubkey == pubkey
        } (struct<auto_undelegation> ( $, pubkey ))
    );
    update delegation_state @* {
        .current_provider.pubkey == pubkey
    } (
        .previous_provider = .current_provider,
        .current_provider = null_provider,
        .current_active_from = current_time,
        .next_provider = null_provider,
        .next_change_at = 0
    );
}

function _ensure_delegation_state(_staker_state: staker_state) {
    return delegation_state @? { _staker_state } ?: create delegation_state (
        _staker_state,
        current_provider = provider @ { get_null_account() }
    );
}

// Deposits and balances

function _staking_deposit_native_impl(amount: integer, delegate_to: pubkey? = null) {
    require(amount > 0, "Can't deposit nothing (or less)!");

    val staker = ft4.auth.authenticate();
    require_non_memo_account(staker);

    // Pull in the CHR
    require(amount <= get_account_balance(staker), "Insufficient funds to deposit.");
    ft4.assets.Unsafe.transfer(staker, get_deposit_account(), get_asset(), amount);

    var ss = staker_state @? { .account == staker and .network == network.CHR };
    if (exists(ss)) {
        _update_pending_token_time(ss);
        ss.balance += amount;
    } else {
        // Create a fresh staker state
        ss = create staker_state (
            staker,
            network.CHR,
            balance = amount,
            last_update = _staking_last_known_time(),
            pending_token_time = 0
        );

        val ds = _ensure_delegation_state(ss);

        // Find the current delegation at time of deposit and immediately apply it
        val current_delegation = staking_get_current_delegate(staker.id);
        val next_delegation = staking_get_next_delegation_change(staker.id);

        update ds (
            .current_provider = provider @ { current_delegation ?: get_null_account() },
            .next_provider = provider @ { next_delegation?.pubkey ?: get_null_account() },
            .next_change_at = next_delegation?.time ?: 0,
            .current_active_from = _staking_last_known_time()
        );
    }

    log("Processed a native CHR deposit of %s for %s".format(amount, staker.id));

    if (delegate_to != null) {
        // Down this path everything is updated anyway
        _staking_delegate(staker, delegate_to);
    } else {
        // Deactivate withdraw requests in any case
        update staker_withdrawal_request @* {
             .active and .staker_state.network == network.CHR and .staker_state.account == staker
        } ( .active = false );
    }

    publish_staking_state(staker);
}

function _change_staking_account(account: ft4.accounts.account, new_account: ft4.accounts.account) {
    val count = staker_state @? { account } ( @sum 1 ) ?: 0;
    update staker_state @* { account } ( .account = new_account );

    log("Changed staking account from %s to %s and migrated %s staker states".format(
        account.id, new_account.id, count));

    publish_staking_state(account);
    publish_staking_state(new_account);
}


// Legacy EVM events imports

function _staking_update_evm_state(staker: ft4.accounts.account, network, new_balance: integer) {
    log("Staking state update of %s to %s on network %s".format(staker.id, new_balance, network));

    val null_provider = provider @ { get_null_account() };
    var ss = staker_state @? { .account == staker and .network == network };
    if (exists(ss)) {
        _update_pending_token_time(ss);
    } else {
        // Create a fresh staker state
        ss = create staker_state (
            staker,
            network,
            balance = 0,
            last_update = _staking_last_known_time(),
            pending_token_time = 0
        );
    }
    val ds = _ensure_delegation_state(ss);
    if (ss.balance == 0 and new_balance > 0) {
        // Find the current delegation at time of deposit and immediately apply it
        val current_delegation = staking_get_current_delegate(staker.id);
        val next_delegation = staking_get_next_delegation_change(staker.id);

        update ds (
            .current_provider = provider @ { current_delegation ?: get_null_account() },
            .next_provider = provider @ { next_delegation?.pubkey ?: get_null_account() },
            .next_change_at = next_delegation?.time ?: 0,
            .current_active_from = _staking_last_known_time()
        );
    }

    // Handle the balance disappearing, eg. a withdrawal
    if (0 == new_balance) {
        log("Detected an EVM withdrawal for %s on network %s".format(staker.id, network));

        update ds (
            .current_provider = null_provider,
            .current_active_from = _staking_last_known_time(),
            .next_provider = null_provider,
            .next_change_at = 0
        );
    }
    update staker_withdrawal_request @* {
            .active
        and .staker_state.network == network
        and .staker_state == ss
    } (
        .active = false
    );

    ss.balance = new_balance;
    publish_staking_state(staker);
}


// Withdrawals

function _staking_request_withdrawal(user: ft4.accounts.account, _network: network) {
    val ss = staker_state @? { .account == user and .network == _network };
    require(ss, "No staker state for %s".format(user.id));

    val last_known_time = _staking_last_known_time();

    val active_withdraw_request = staker_withdrawal_request @? {
        ss,
        .active
    };

    if (_network == network.CHR) {
        require(not exists(active_withdraw_request), "Only one withdrawal request allowed at a time");
    }
    require(ss.balance > 0, "Balance must be over 0");

    // Update appropriate staker states with null provider
    val null_provider = require(provider @? { get_null_account() }, "No provider for this public key.");
    val current_delegate = staking_get_current_delegate_on_network(user.id, _network);
    val next_change_at = last_known_time + chain_context.args.staking_delegation_delay_ms;

    val ds = _ensure_delegation_state(ss);
    update ds (
        .current_provider = provider @ { current_delegate ?: get_null_account() },
        .current_active_from = if (.next_change_at > last_known_time) last_known_time else .next_change_at,
        .next_provider = null_provider,
        .next_change_at = next_change_at
    );

    // Let's consolidate as we've touched the delegation state
    _update_pending_token_time(ss);

    // Make a new withdrawal request
    create staker_withdrawal_request (
        finish_at = last_known_time + chain_context.args.staking_withdrawal_delay_ms,
        ss,
        true
    );

    publish_staking_state(user);

    log("Created a withdrawal request for %s on network %s".format(user.id, _network));
}

function _staking_distribute_withdrawals() {
    val open_withdrawals = staker_withdrawal_request @* {
        .active and .staker_state.network == network.CHR and .finish_at <= _staking_last_known_time()
    };

    if(open_withdrawals.empty()) return;

    log("Preparing to pay out %s withdrawals.".format(open_withdrawals.size()));

    // Find all the staker states concerned
    val staker_states = staker_withdrawal_request @* {
        .active and .staker_state.network == network.CHR and .finish_at <= _staking_last_known_time( )
    } ( .staker_state );

    val null_provider = provider @ { get_null_account() };

    // Clear future delegation states (not the current one)
    update delegation_state @* { .staker_state in staker_states } ( .current_provider = null_provider, .current_active_from = _staking_last_known_time(), .next_provider = null_provider, .next_change_at = 0 );


    // Process each withdrawal
    for (withdrawal in open_withdrawals) {
        // Send the balance back to them through ft4
        ft4.assets.Unsafe.transfer(
                get_deposit_account(),
                withdrawal.staker_state.account,
                get_asset(),
                withdrawal.staker_state.balance
            );
        log("Withdrawal of amount %s sent back to %s.".format(withdrawal.staker_state.balance, withdrawal.staker_state.account));
    }

    update staker_states ( .balance = 0 );
    update staker_withdrawal_request @* {
        .active and .staker_state.network == network.CHR and .finish_at <= _staking_last_known_time( )
    } ( .active = false);

    for (ss in staker_states) {
        publish_staking_state(ss.account);
    }
}


// Delegation

function _staking_delegate(staker: ft4.accounts.account, provider_pubkey: pubkey, _network: network? = null) {

    val new_provider = require(provider @? { provider_pubkey }, "No provider for this public key.");
    require(new_provider.tier == provider_tier.NODE_PROVIDER, "Provider is not node provider");
    val last_known_time = _staking_last_known_time();

    // Get all staker states to change of user, and all related withdraw requests
    val active_states = delegation_state @* {
        .staker_state.account == staker,
        (.staker_state.network == _network or empty(_network)),
        .staker_state.balance > 0,
        ((.next_change_at > last_known_time or .next_change_at == 0) and .current_provider != new_provider)
        or
        ((.next_change_at <= last_known_time and .next_change_at > 0) and .next_provider != new_provider)
    } ( s = .staker_state, p = if (.next_change_at <= last_known_time and .next_change_at > 0) .next_provider else .current_provider );
    
    val staker_states = active_states @* { } ( .s );
    val withdraw_requests = staker_withdrawal_request @* { .staker_state in staker_states and .active };

    // deactivate chromia withdraw requests
    val withdraw_requests_chr = withdraw_requests @* { .staker_state.network == network.CHR };
    if (not empty(withdraw_requests_chr)) {
        update withdraw_requests_chr ( .active = false );
        publish_staking_state(staker);
    }

    val staker_states_to_exclude = withdraw_requests @* { .staker_state.network != network.CHR } ( .staker_state );
    val included_states = active_states @* { .s not in staker_states_to_exclude } ( .s );
 
    val old_provider_pubkey = staking_get_current_delegate(staker.id);
    val old_provider = provider @ { old_provider_pubkey ?: get_null_account() };
    val null_provider = provider @ { get_null_account() };
    // Delegation from nothing happens without the delay
    val new_time_offset = if (empty(old_provider_pubkey)) 0 else chain_context.args.staking_delegation_delay_ms;
    val new_active_time = last_known_time + new_time_offset;

    require(active_states @ { .s in included_states } ( @sum 1 ) > 0, "No available staker states to delegate");

    if (empty(old_provider_pubkey)) {
        update delegation_state @* {
            .staker_state in included_states
        } (
            .current_provider = new_provider,
            .current_active_from = last_known_time,
            .next_provider = null_provider,
            .next_change_at = 0
        );
    } else {
        update delegation_state @* {
            .staker_state in included_states
        } (
            .current_provider = old_provider,
            .current_active_from = if (.next_change_at <= last_known_time and .next_change_at > 0) .next_change_at else .current_active_from,
            .next_provider = new_provider,
            .next_change_at = new_active_time
        );
    }
}

function _get_current_provider_pubkey(ds: delegation_state?) {
    if (empty(ds)) return null;

    val provider = _get_current_provider(ds);
    if (provider.pubkey == get_null_account()) return null;
    return provider.pubkey;
}

function _get_current_provider(ds: delegation_state) {
    return when {
        ds.next_change_at == 0 -> ds.current_provider;
        ds.next_change_at <= _staking_last_known_time() -> ds.next_provider;
        else -> ds.current_provider;
    };
}

function _staking_distribute_rewards(using_batches: boolean = false, update_pending_token_times: boolean = true) {
    val native_rate = staking_get_current_rate() / millis_per_year;
    val evm_rate = staking_get_current_evm_rate() / millis_per_year;

    // Get a list of payouts
    if (update_pending_token_times or not using_batches) {
        _update_pending_token_time_all();
    }

    val all_staking_rewards = staker_state @* {
        .pending_token_time > 0
    } (
        @group .account,
        @sum amount = integer (
            (
            .pending_token_time
        ) * (if (.network == network.CHR) native_rate else evm_rate) * units_per_asset
        )
    ) limit (
        if (using_batches) chain_context.args.staking_rewards_payout_batch_size else integer.MAX_VALUE
    );

    val all_users_for_batch = all_staking_rewards @* {} (.account);

    val all_shared_rewards = pending_shared_payment @* {
            .account in all_users_for_batch
        and .amount > 0
    } (
        @group .account,
        @sum shared_amount = .amount,
        @set provider_set = .provider_pubkey
    );

    val total_amount = all_staking_rewards @ { } ( @sum .amount );
    log("Preparing to distribute %s rewards worth %s".format(all_users_for_batch.size(), total_amount));

    val total_shared_amount = all_shared_rewards @ { } ( @sum .shared_amount );
    val total_shared_rewarded_accounts = all_shared_rewards @ {} ( @sum 1 );
    log(
        "Preparing to distribute %s shared rewards worth %s"
            .format(
                total_shared_rewarded_accounts,
                total_shared_amount
            )
    );

    // Do not start what we cannot finish
    val pool = get_pool_account();
    var total_pool_balance = get_account_balance(pool);
    if (total_pool_balance < total_amount + total_shared_amount) {
        total_pool_balance = refill_pool_account_if_possible();
        if (total_pool_balance < total_amount + total_shared_amount) {       
            log("Cannot pay rewards. Insufficient funds.");
            return;
        }
    }

    val asset = get_asset();
    val last_known_time = _staking_last_known_time();

    // Send payouts
    for ((account, amount) in all_staking_rewards ) {
        val shared = all_shared_rewards @? {.account == account} (.shared_amount, .provider_set) ?: (shared_amount = 0, provider_set = set<byte_array>());
        if (not try_call(_staking_pay_reward(last_known_time, pool, asset, account, amount, shared.shared_amount, list<byte_array>(shared.provider_set), *))) {
            log("Failed to pay out %s including %s from shared rewards to %s".format(amount + shared.shared_amount, shared.shared_amount, account.id));
        }
    }
    log("Stored payments on-chain.");
}

function _staking_pay_reward(last_known_time: timestamp, pool: ft4.accounts.account, ft4.assets.asset, account: ft4.accounts.account, amount: integer, shared_amount: integer, providers: list<byte_array>) {
    val total_amount = amount + shared_amount;
    update staker_state @* { account } ( .pending_token_time = 0 );
    delete pending_shared_payment @* { account };

    if (total_amount == 0) {
        log("Total staking reward for %s was 0. Nothing to pay out.".format(account.id));
        return;
    }

    val provider = if (empty(providers)) x'' else providers[0];
    ft4.assets.Unsafe.transfer(pool, account, asset, total_amount);
    create staking_payment(timestamp = last_known_time, amount = amount, account, shared_amount, provider);
    log("Paid out reward of %s including %s staking reward and %s shared reward to %s".format(total_amount, amount, shared_amount, account.id));
}

// Shared rewards (triggered by a hook in the rewards logic)

function _staking_shared_rewards(pay_reward_result, delegation_amount: big_integer) {
    val last_known_time = _staking_last_known_time();

    // Do nothing if the start date has not passed
    if (last_known_time < staking_get_rewards_start()) {
        return;
    }

    // Find provider
    val provider_data = provider_account @? { pay_reward_result.payed_to } (provider = .provider, pubkey = .provider.pubkey);

    // In some edge cases during testing a public key may be absent
    if (empty(provider_data)) {
        log("Not provider found for id: %s".format(pay_reward_result.payed_to.id));
        return;
    }

    val evm_staking_enabled = staking_get_current_evm_rate() > 0;
    val staker_delegations = delegation_state @* {
        (evm_staking_enabled or .staker_state.network == network.CHR) and
        ((.current_provider == provider_data.provider and (.next_change_at > _staking_last_known_time() or .next_change_at == 0)) or
        (.next_provider == provider_data.provider and .next_change_at <= _staking_last_known_time()))
    } (
        @group account = .staker_state.account,
        @sum balance = .staker_state.balance
    );

    // Get total and divide through
    val total_delegation = staker_delegations @ { } ( @sum .balance );

    // Zero delegation check (should not occur in normal useage.)
    if (total_delegation == 0) {
        return;
    }

    val pending_shared_payment_structs = staker_delegations @* { } (
        struct<pending_shared_payment> (
            .account,
            // @dev The decimal is important
            amount = integer(delegation_amount * (.balance / decimal(total_delegation))),
            provider_data.pubkey,
        )
    );

    create pending_shared_payment ( pending_shared_payment_structs );
    log("Created %s pending shared payments for delegation to provider %s".format(pending_shared_payment_structs.size(), provider_data.pubkey));
}
