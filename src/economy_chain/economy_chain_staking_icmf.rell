function publish_staking_balance(staker: ft4.accounts.account) {
    val balance = staker_state @ { staker } ( @sum .balance );
    send_message(
        economy_chain_staking_balance_update_topic,
        economy_chain_staking_balance_update(
            staker.id,
            balance,
            ).to_gtv()
    );
}
