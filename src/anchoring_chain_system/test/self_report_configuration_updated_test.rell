@test module;

import ^.helpers.*;
import ^^.*;

function test_self_report() {
    rell.test.tx().op(
        dummy_op(0)
    ).run();

    rell.test.tx().op(
        dummy_op(1)
    ).run();

    // TODO update config when possible https://chromaway.atlassian.net/browse/RELL-86?focusedCommentId=15620

    rell.test.tx().op(
        dummy_op(2)
    ).run();
}
