module;

/**
 * The purpose of this modules it to keep global constants.
 */

val millis_per_day = 24 * 60 * 60 * 1000;
val millis_per_week = 7 * millis_per_day;
val millis_per_month = 30 * millis_per_day;
val millis_per_year = 365 * millis_per_day;
