module;

@extendable function direct_cluster(): boolean = false;
@extendable function direct_container(): boolean = false;

query has_direct_cluster(): boolean = direct_cluster();

query has_direct_container(): boolean = direct_container();