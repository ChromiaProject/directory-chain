module;

import ^^.*;

// Query wrapped in func to be able to assert require() call
function _query_in_fun__nm_get_container_for_blockchain(blockchain_rid: byte_array) {
    nm_get_container_for_blockchain(blockchain_rid);
}

operation _add_configuration_hash(blockchain_rid: byte_array, height: integer, config_hash: byte_array) {
    val bc = require_blockchain(blockchain_rid);
    create blockchain_configuration_hash(bc, height, config_hash);
}
