/**
 * The purpose of this module was previously to provide a `schedule_at` framework. Over time this has been replaced by
 * the scheduler in the `begin_block` library. Now this only manages directory chain proposals and the part before they
 * are approved, to keep track of `schedule_at` times that are later used to schedule applying the proposal.
 */

module;

import lib.begin_block.*;
import common_helpers.block.*;
import model.*;

enum delay_type {
    PROPOSAL,
}
