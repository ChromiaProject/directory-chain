module;

struct module_args {
    /** Release/tag version */
    release_version: text = "dev";

    /** Commit hash */
    release_commit: text = "dev";
}

query release_version() = chain_context.args.release_version;

query release_commit() = chain_context.args.release_commit;
