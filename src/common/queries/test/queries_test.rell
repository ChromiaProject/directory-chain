@test module;

import ^^.*;

struct module_args {
    developers: gtv? = null;
}

function test_get_developers() {
    val developers = get_developers();

    assert_equals(developers, ["first.last@chromaway.com"]);
}