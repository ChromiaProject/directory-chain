module;

import common.*;
import model.*;
import proposal.*;

function validate_digest(digest: text) {
    require(
        digest.matches("^[a-zA-Z0-9:]{1,100}$"),
        "Digest name can only contain letters, numerals, and colon. Maximum allowed length is 100 characters."
    );
}
