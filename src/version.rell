module;

// Increment this everytime a query/op in directory chain is updated or changed, but not more than once per release.
// Should match the minor release version, e.g. release 1.17.5 should have api_version 17.
query api_version(): integer = 110;
