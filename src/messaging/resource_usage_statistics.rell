module;

import lib.icmf.constants.*;

val resource_usage_statistics_topic = ICMF_TOPIC_GLOBAL_PREFIX + "resource_usage_statistics";

struct resource_usage_statistics_message {
    node_pubkey: pubkey;
    container_name: text;
    measurement_time: timestamp;
    type: text;
    metric_value: decimal;
}
