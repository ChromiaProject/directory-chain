module;

import lib.icmf.constants.*;

val container_blockchain_update_topic = ICMF_TOPIC_LOCAL_PREFIX + "container_blockchain_update";

struct container_blockchain_update_message {
    container_name: text;
    blockchain_rid: byte_array;
    deleted: boolean;
}