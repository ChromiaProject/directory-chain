@test module;

import o: ^.ops;
import c: ^.util.constants;
import ^^^.model.*;

function initial_provider() = provider @ { c.initial_provider.pub };

function setup_module() {
    rell.test.tx().op(o.initialize_test_module(c.initial_provider.pub)).run();
}
