module;

import ^^.*;

// Query wrapped in op to be able to assert require() call
operation _query_in_op__nm_get_container_for_blockchain(blockchain_rid: byte_array) {
    nm_get_container_for_blockchain(blockchain_rid);
}
